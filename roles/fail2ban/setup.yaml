---

#Commandline : 
#ansible-playbook /etc/ansible/roles/postinstall/setup.yml

- name: 'Yet another Ansible playbook to install fail2ban with ssh/ssh_ddos jails'
  become: true
  become_user: root
  become_method: sudo
  roles:
    - fail2ban